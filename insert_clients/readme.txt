- Correr el siguiente código para activar el ambiente que tiene los paquetes necesarios:

./myenv/Scripts/activate

- Luego poner la contraseña que usted introduce para entrar a SQL Server
en la variable 'password' ubicada: "src\consts\password_d.py" y guardar.

- En la función 'make_connection' cambiar 'DIONISIO' por 'APOLO' y guardar.

- Correr el archivo main.py para que agregue los clientes.

- Cuando haya terminado de correr el código solo tiene que correr en la termina lo siguiente:

deactivate